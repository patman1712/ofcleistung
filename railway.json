{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile",
    "buildCommand": ""
  },
  "deploy": {
    "startCommand": "",
    "healthcheckPath": "/api/auth/me",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "volumes": [
      {
        "name": "ofc-sqlite-data",
        "mountPath": "/app/data"
      }
    ]
  }
}
