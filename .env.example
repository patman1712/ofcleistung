# SQLite (automatisch, keine extra DB nötig)
DATABASE_URL="file:./data/ofc.db"

# Alternativ: PostgreSQL (Railway kann weiterhin genutzt werden, wenn gesetzt)
# DATABASE_URL="postgresql://user:password@localhost:5432/ofc_leistungsdiagnostik?schema=public"

JWT_SECRET="change-me-in-production-please-change-this-secret-key"
PORT="3000"
NODE_ENV="development"
